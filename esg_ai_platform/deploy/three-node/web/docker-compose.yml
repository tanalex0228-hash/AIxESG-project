name: aixesg-web-proxy

services:
  nginx:
    image: nginx:1.27-alpine
    container_name: aixesg_nginx
    restart: unless-stopped
    ports:
      - "80:80"
    volumes:
      - ./nginx.conf:/etc/nginx/conf.d/default.conf:ro

